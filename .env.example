# Google Apps Script web app URL (…/exec). Server proxy reads this first:
APPS_SCRIPT_LEAD_URL=
# Same URL here enables a browser fallback if /api/lead fails (404/5xx/503). Exposed in client JS.
# NEXT_PUBLIC_APPS_SCRIPT_LEAD_URL=
